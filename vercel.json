{
  "installCommand": "npm install && npm install --no-save @vercel/next",
  "buildCommand": "NEXT_ADAPTER_PATH=$(node -e \"console.log(require.resolve('@vercel/next'))\") npx next build --webpack",
  "framework": "nextjs"
}
